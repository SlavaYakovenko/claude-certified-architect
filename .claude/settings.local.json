{
  "permissions": {
    "allow": [
      "Bash(awk '{print NR+5, length\\($0\\), $0}')",
      "FileSystem.read(./**)",
      "FileSystem.write(./**)"
    ],
    "deny": [
      "git commit",
      "git push",
      "git remote *"
    ]
  }
}
